This zip is the handoff package for Codex.

Start with `HANDOFF_TO_CODEX.md`.
The clean baseline code is in `odyssey_repo_package/`.
The screenshot files show:
- current bad local result
- rejected v17-style direction
- preferred flow-first target direction
